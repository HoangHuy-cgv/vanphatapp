# Handoff — Cleanup xong, commit 47a711c (SSOT rolling, <25 dòng)

> Commit: `47a711c` ignore build churn + fix drawer chết (tiếp `993784c`).
> Quy ước: Sếp/em, tiếng Việt. Không `git push` khi chưa lệnh (chưa có remote).

- Ignore assets hash-churn; worktree sạch 0 modified/untracked.
- DrawerCustomerDetail đọc bảng con native (13 TP DS COSMETIC có data).
- Plan/todo hết stale; browser 404/errors 0, SP badge 88.
- Còn theo plan: remote+push, bench staging, enforce BTP, MST, CI/test.
