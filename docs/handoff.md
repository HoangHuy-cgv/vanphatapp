# Handoff — Tab SP gộp xong, commit 715d60d (SSOT rolling, <25 dòng)

> Commit: `715d60d` tên quen gọn + tab SP 88 mã (tiếp `f739883`).
> Quy ước: Sếp/em, tiếng Việt. Không `git push` khi chưa lệnh.

- Bảng: chỉ tên gọi quen (gỡ mã biến thể trùng); search vẫn nhận cả 2.
- Drawer giữ mã biến thể để đối chiếu giấy tờ KH; mã nội bộ tooltip.
- Tab Sản phẩm: TP 45 + NGCS 15 + TMD 12 + BTP 16 = 88 (qb OR native).
- Verify: browser 404/errors 0, badge 88, first NGCS Nhỏ Đỏ - Đam Mê.
- Mock: http://127.0.0.1:8080/portal (log /tmp/portal-server.log).
- Còn: bench staging ERPNext thật + `git push` (chờ lệnh Sếp).
