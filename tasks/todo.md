# TODO — slice cleanup (docs/code/rule cùng ngữ cảnh)

Đang làm: dọn rác + dead code + simplify (CSS chết, biến submit chết, ảnh mẫu cứng,
`defineExpose` chết, `loadAllCatalogData` tải thừa, JSON mock local) + cập nhật
docs/rule/policy (README, specs/README, plan, handoff, SPEC variant) cùng ngữ cảnh
triple rule → verify → commit.

Không thuộc slice này (chờ Sếp): cổng quyền 22 endpoint, bench staging p95, Vitest.
