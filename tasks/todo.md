# TODO — việc hiện tại (Sếp ra việc mới làm)

- Chờ Sếp chọn slice tiếp theo: flow cọc 2 bước (Sales yêu cầu → Kế toán xác nhận),
  Vitest frontend, hoặc tối ưu đường truyền (Argo/cache edge/tách detail).
- Mặc định: giữ worktree sạch, verify xanh (48 test + floor + guard + budget).
